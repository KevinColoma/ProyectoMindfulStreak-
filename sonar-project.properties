# Claves del proyecto en SonarCloud
sonar.projectKey=KevinColoma_ProyectoMindfulStreak-
sonar.organization=kevincoloma

# Analiza desde la raíz del proyecto
sonar.sources=.

# Codificación de los archivos fuente
sonar.sourceEncoding=UTF-8

# Exclusiones comunes (muy recomendable para no analizar librerías o archivos generados)
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/*.min.js,**/*.spec.js

# Si deseas que analice TODO, puedes comentar esta línea anterior. Pero es mejor dejarla.

# Incluir todos los tipos de archivo relevantes (opcional, si quieres forzar extensiones específicas)
# sonar.inclusions=**/*.js,**/*.ts,**/*.jsx,**/*.tsx,**/*.py,**/*.java

# Detecta todos los lenguajes soportados (esto lo hace automáticamente si tienes los plugins)
# No es necesario sonar.language (obsoleto)
